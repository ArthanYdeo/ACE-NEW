import 'package:flutter/material.dart';

class DetailPage6 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: Center(
        child: Text('Class Detail 5'),
      ),
    );
  }
}
